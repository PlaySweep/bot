require 'rubotnik'
Rubotnik::Autoloader.load('bot')
Dir.glob('bot/api.rb').each { |r| load r}
Dir.glob('bot/requests.rb').each { |r| load r}
Dir.glob('bot/variables.rb').each { |r| load r}
Dir.glob('bot/lib/tasks/*.rake').each { |r| load r}
